<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.cms.module.ocr.mapper.ExpenseRequestMapper">
	<insert id="insert"
		parameterType="com.cms.module.ocr.entity.ExpenseRequest"
		useGeneratedKeys="true" keyProperty="id">
		INSERT INTO expense_request
		(applicant_id, summary, status, approved_by, approved_at, total_amount, created_at)
		VALUES
		(#{applicantId}, #{summary}, #{status}, #{approvedBy}, #{approvedAt}, #{totalAmount},
		#{createdAt})
	</insert>

	<select id="sumReceiptAmounts" resultType="long">
		SELECT COALESCE(SUM(amount),0)
		FROM receipt_info
		WHERE id IN
		<foreach collection="ids" item="id" open="(" separator=","
			close=")">
			#{id}
		</foreach>
	</select>
</mapper>