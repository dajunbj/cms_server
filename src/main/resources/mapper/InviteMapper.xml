<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cms.module.invite.mapper.InviteMapper">
	
	<insert id="insert" parameterType="map">
        INSERT INTO employees (
            name,
            gender,
            date_of_birth,
            phone_number,
            email,
            login_id,
            user_role,
            department_id,
            pwd
        ) VALUES (
            #{name},
            #{gender},
            #{date_of_birth},
            #{phone_number},
            #{email},
            #{login_id},
            #{role},
            #{department_id},
            #{pwd}
        )
    </insert>

</mapper>